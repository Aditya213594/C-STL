Syntax : vector<datatype/container> vectorName;
for eg. vector<int> v;

Input : 
for eg. 
int n;
cin>>n;

for(int i=0;i<n;i++)
{
    int x;
    cin>>x;
    v.push_back(x);
}

Print :
for(int i=0;i<v.size;i++)
{
    cout << v[i] << "\n";    
}